# CryoTrader — Project Context for AI Agents

**Version:** 1.14.0
**Last Updated:** April 2026
**Python:** 3.9+ (no 3.10+ syntax — use `Optional[X]`, not `X | None`)
**Venv:** `.venv` — use `.venv/bin/python3` directly

Automated BTC options trading bot with multi-exchange support (Coincall + Deribit).
Config-driven strategies, tick-based execution, slot-based multi-strategy deployment.

---

## ⚠️ Hard Rules

1. **Never deploy to production** without explicit user approval.
2. **Never `git commit` or `git push`** without explicit user approval.
3. **For any non-trivial task: present a plan first.** Wait for user to say "go".
4. **Run tests before and after any code change:** `python -m pytest tests/ -v`

---

## ⚠️ Two Separate Applications in One Repo

This repo contains **two largely independent applications**:

| | **Live Trading Engine** | **Backtester** |
|---|---|---|
| Root | `/` (top level) | `backtester/` |
| Entry | `main.py` | `backtester/run.py` |
| Strategies | `strategies/` | `backtester/strategies/` |
| Config | `config.py`, `.env` | `backtester/config.py`, `backtester/config.toml` |
| Engine | `lifecycle_engine.py` | `backtester/engine.py` |
| Market data | `market_data.py` | `backtester/market_replay.py` |

Many modules share similar or identical names. Always confirm which application before editing.

---

## Current Development Focus (April 2026)

- **Strategy development & refinement** — `strategies/`, `strategy.py`, `lifecycle_engine.py`
- **Dashboard improvements** — `dashboard.py`, `hub/`
- **Logging, persistence, trade blotter** — `logging_setup.py`, `persistence.py`
- **Indicators module** — `indicators/`; `turbulence.py` is the reference implementation
- **Backtester future** — possible separation into its own repo; rich GUI under consideration

---

## 1  Architecture

```
PositionMonitor (10s poll, daemon thread)
  │
  ├─► LifecycleEngine.tick(snapshot)   — advances every active trade's state machine
  │
  └─► StrategyRunner.tick(snapshot)     — per strategy:
        1. check_closed_trades()        — fire on_trade_closed callback
        2. entry gate (all conditions)  — time, weekday, margin, equity, delta
        3. resolve_legs()               — LegSpec → concrete TradeLeg via option_selection
        4. lifecycle_engine.create()    — creates TradeLifecycle with exit conditions
        5. lifecycle_engine.open()      — routes to limit / rfq executor
```

### Trade State Machine

```
PENDING_OPEN → OPENING → OPEN → PENDING_CLOSE → CLOSING → CLOSED
                                                           └─► FAILED
```

Exit conditions are evaluated every tick while OPEN. Any single exit returning True triggers PENDING_CLOSE.

### Design Principles
1. Composition over inheritance — callables, not class hierarchies
2. Dataclasses everywhere — typed data containers, frozen for thread-safe snapshots
3. Single responsibility — one module per concern
4. Tick-driven — no event queues or extra threads (except PositionMonitor + dashboard)
5. Side encoding: all internal code uses `"buy"` / `"sell"` strings. Exchange adapters convert at API boundary.

---

## 2  Exchange Abstraction

### ABC Interfaces (`exchanges/base.py`)
- `ExchangeAuth` — `reachable`, `get()`, `post()`, `is_successful()`
- `ExchangeMarketData` — `get_index_price()`, `get_option_instruments()`, `get_option_details()`, `get_option_orderbook()`
- `ExchangeExecutor` — `place_order(symbol, qty, side: str, ...)`, `cancel_order()`, `get_order_status()`
- `ExchangeAccountManager` — `get_account_info()`, `get_positions()`, `get_open_orders()`
- `ExchangeRFQExecutor` — `execute()`, `execute_phased()`, `get_orderbook_cost()`

### Factory (`exchanges/__init__.py`)
`build_exchange(name)` → returns dict with `auth`, `market_data`, `executor`, `account_manager`, `rfq_executor`, `state_map`. Reads `config.EXCHANGE` (default `"coincall"`).

### Coincall Adapter (`exchanges/coincall/`)
Thin delegation wrappers — no business logic. `CoincallExecutorAdapter` converts `"buy"`/`"sell"` → `1`/`2` ints. Auth: HMAC-SHA256.

### Deribit Adapter (`exchanges/deribit/`)
Full implementation — all 7 modules complete:
- **Auth:** OAuth2 client_credentials, proactive token refresh at 80% TTL, thread-safe
- **JSON-RPC:** All calls via `DeribitAuth.call(method, params)` — JSON-RPC 2.0 envelope, Bearer token for private methods
- **Price normalization:** BTC-denominated → USD conversion at adapter boundary
- **Tick sizes:** `_snap_to_tick()` — price < 0.005 BTC → tick 0.0001; ≥ 0.005 → tick 0.0005
- **Symbol translation:** `coincall_to_deribit()` / `deribit_to_coincall()` — e.g. `BTCUSD-03APR26-74000-C` ↔ `BTC-3APR26-74000-C`
- **RFQ:** Block RFQ create→poll→accept; 25 BTC minimum
- **Env vars:** `DERIBIT_CLIENT_ID`, `DERIBIT_CLIENT_SECRET`, `DERIBIT_BASE_URL`
- **URLs:** testnet `https://test.deribit.com`, prod `https://www.deribit.com`

---

## 3  Module Map

### Core
| File | Purpose |
|------|---------|
| `main.py` | Entry point, crash recovery (order ledger + trade snapshot + reconcile), dynamic strategy import via `SLOT_STRATEGY` |
| `strategy.py` | `TradingContext` DI, `build_context()`, `StrategyConfig`, `StrategyRunner`, entry/exit condition factories |
| `trade_lifecycle.py` | Data-only: `TradeState`, `TradeLeg`, `TradeLifecycle`, `RFQParams`, `ExitCondition`, PnL helpers |
| `lifecycle_engine.py` | `LifecycleEngine`: state machine, tick, creates/owns `ExecutionRouter` + `OrderManager` |
| `execution_router.py` | Routes open/close to limit/rfq/smart, mode auto-detect, circuit breaker |
| `order_manager.py` | Central order ledger: idempotent placement, supersession chains, JSONL audit, reconcile |
| `trade_execution.py` | `TradeExecutor`, `LimitFillManager`, `ExecutionParams`, `ExecutionPhase`. 6 pricing modes |
| `rfq.py` | `RFQExecutor` — atomic multi-leg RFQ for $50k+ notional |
| `option_selection.py` | `LegSpec`, `resolve_legs()`, `find_option()`, `straddle()`, `strangle()` |
| `market_data.py` | Option chains, orderbooks, BTC price; 30s TTL cache |
| `account_manager.py` | `AccountManager`, `AccountSnapshot`, `PositionSnapshot`, `PositionMonitor` |
| `config.py` | `.env` loading, `EXCHANGE` selection |
| `auth.py` | Coincall HMAC-SHA256 + `@retry` |
| `ema_filter.py` | Binance kline EMA-20 filter, 1h cache, entry condition factory |

### Infrastructure
| File | Purpose |
|------|---------|
| `persistence.py` | Append-only `trade_history.jsonl` |
| `health_check.py` | Background health logging (5 min interval) |
| `telegram_notifier.py` | Fire-and-forget alerts, `get_notifier()` singleton |
| `dashboard.py` | Flask + htmx per-slot web dashboard |
| `position_closer.py` | Kill switch: two-phase mark-price closer + cancel_all |
| `slot_config.py` | TOML → .env generator for slot deployment |
| `accounts.toml` | Named exchange accounts (no secrets) |

---

## 4  Strategies

All strategies use `_p("NAME", default, cast)` helper → reads `PARAM_<NAME>` env vars.
Register in `strategies/__init__.py`, select via `SLOT_STRATEGY` env var.

| File | Description | Exchange | Active |
|------|-------------|----------|--------|
| `put_sell_80dte.py` | Sell OTM put at -0.15δ, monthly expiry ≥80 DTE, entry 13–14 UTC, TP=95%, SL=250%, EMA-20 filter | Coincall | **slot-01** |
| `short_strangle_delta_tp.py` | Sell OTM strangle at target δ; TP + min_otm_pct guard | Deribit | **slot-02** |
| `short_strangle_delta.py` | Delta-selected strangle; weekend filter | Deribit | Base |
| `daily_put_sell.py` | 1DTE -0.10δ put, entry 03–04 UTC (legacy) | Coincall | — |
| `long_strangle_index_move.py` | Long ±$2000 OTM strangle; exit on $1500 index move or time | Deribit | — |
| `atm_straddle_index_move.py` | Long ATM straddle; exit on BTC move ≥$1200 or time | Either | — |
| `short_straddle_strangle.py` | Combined short straddle/strangle | Either | — |
| `blueprint_strangle.py` | Template for new strategies | Either | Template |

### Active Production Slots
| Slot | Strategy | Exchange | Key Params |
|------|----------|----------|------------|
| slot-01 | `put_sell_80dte` | Coincall | qty=0.1, δ=-0.15, dte≥80, entry 13–14 UTC |
| slot-02 | `short_strangle_delta_tp` | Deribit | qty=5, δ=0.15, dte=1, entry 18 UTC, SL=5×, TP=80% |

---

## 5  Execution Pipeline

| Mode | Module | When | How |
|------|--------|------|-----|
| `limit` | `trade_execution.py` | Default / small trades | Per-leg limit orders via `LimitFillManager`. Phased pricing: `ExecutionPhase(pricing, duration, buffer_pct, reprice_interval)`. Best-effort close: skip bad-priced legs rather than aborting. |
| `rfq` | `rfq.py` | $50k+ notional | Atomic multi-leg RFQ. `RFQParams` configures timeout, min improvement, fallback. |
| `auto` | `execution/router.py` | Default mode | Routes by notional: rfq ≥ $50k, else limit. |

---

## 6  Dashboard & Hub

### Per-Slot Dashboard (`dashboard.py`)
Flask + htmx, daemon thread. Session auth with `DASHBOARD_PASSWORD`, brute-force lockout, `secrets.compare_digest()`.

**Dashboard modes** (`DASHBOARD_MODE` env var): `full` (default UI), `control` (headless, localhost-only `/control/*` endpoints), `disabled`.

**Control endpoints** (localhost-only, used by hub): `/control/status`, `/control/pause|resume|stop|kill`

**Kill switch** (`/api/killswitch`): Two-phase mark-price close via `PositionCloser`.

### Hub Dashboard (`hub/hub_dashboard.py`)
Separate Flask app at `/opt/ct/hub/`, port 8070. Aggregates all slots.
- Discovers slots by scanning `/opt/ct/slot-*` directories
- Proxies control commands to slot localhost ports (808X)
- Tick recorder health card via `RECORDER_HEALTH_PORT`
- Routes: `/`, `/api/overview`, `/api/recorder`, `/api/slot/<id>/detail|logs|pause|resume|stop|kill`

---

## 7  Slot Architecture & Deployment

### Slots
Multi-strategy deployment via isolated "slots" at `/opt/ct/slot-XX/` on production VPS.
Each slot = full codebase copy + own `.env`, `logs/`, venv, systemd service (`ct-slot@XX.service`).

### Slot Config System
- `accounts.toml` — named exchange accounts mapping to env var names (no secrets)
- `slots/slot-XX.toml` — per-slot config: strategy name, account reference, param overrides
- `slot_config.py` — reads TOML → generates `.env.slot-XX`
- Strategy params: `_p("QTY", 0.8)` reads `PARAM_QTY` from env

### Deploy Commands
```bash
./deployment/deploy-slot.sh 01           # deploy + restart slot-01
./deployment/deploy-slot.sh 01 --setup   # first-time slot setup
./deployment/deploy-slot.sh 01 --logs    # tail slot journalctl
./deployment/deploy-slot.sh hub          # deploy hub dashboard
./deployment/deploy-slot.sh status       # overview of all slots
```

### Production Server
- **VPS:** Hetzner CPX22, Ubuntu 24.04, IP `46.225.137.92`
- **Base path:** `/opt/ct/`, slot ports 808X, hub port 8070
- **Config:** `.deploy.slots.env` (gitignored) — `VPS_HOST`, `SSH_KEY`

---

## 8  Backtester (`backtester/`)

Real-data options backtester using Deribit historical tick data.

```bash
python -m backtester.run --strategy <name>
python -m backtester.run --strategy delta_strangle_tp --robustness --wfo
python -m backtester.run --experiment <name>
```

**Runtime model:** parquet snapshots → `MarketReplay` → `run_grid_full()` → `GridResult` → `generate_html()`

### Key Files
| File | Purpose |
|------|---------|
| `backtester/run.py` | CLI entrypoint |
| `backtester/engine.py` | Single-pass grid runner; `run_grid_full()` |
| `backtester/market_replay.py` | Fast iterator; yields `MarketState` |
| `backtester/results.py` | `GridResult`: vectorised scoring (Sharpe, PnL, R², Omega, Ulcer) |
| `backtester/robustness.py` | Deflated Sharpe Ratio (Bailey & López de Prado) |
| `backtester/walk_forward.py` | Walk-forward optimisation |
| `backtester/reporting_v2.py` | Self-contained HTML report |
| `backtester/experiment.py` | Sensitivity analysis from TOML |

### Backtester Strategies (`backtester/strategies/`)
| File | Description |
|------|-------------|
| `straddle_strangle.py` | Long ATM straddle/OTM strangle + index move exit |
| `daily_put_sell.py` | Short OTM put, SL or expiry exit |
| `short_strangle_delta.py` | Delta-selected weekly short strangle |
| `short_strangle_delta_tp.py` | Above + configurable TP + min_otm_pct |
| `short_strangle_weekly_tp.py` | Weekly short strangle with TP/SL |
| `short_strangle_weekly_cap.py` | Above + capacity limits |
| `short_strangle_weekend.py` | Weekend-only variant |
| `deltaswipswap.py` | Long straddle/strangle + dynamic delta hedging (gamma scalping) |

**Data ingestion:** tick recorder (`backtester/ingest/tickrecorder/`) or Tardis bulk download (`backtester/ingest/bulkdownloadTardis/`).

---

## 9  Tick Recorder (`backtester/tickrecorder/`)

Live Deribit tick recorder via WebSocket — produces parquets matching backtester schema.

**Burst-mode capture:** WebSocket stays connected idle with heartbeat. 10s before each 5-min boundary, subscribes to all `ticker.<instrument>.100ms` channels + `deribit_price_index.btc_usd`. Snapshot 0.2s after boundary. Immediately unsubscribes. Writes zstd parquet.

**Data pipeline:** `recorder.py` (live WS) → daily parquets → `sync.py` (rsync from VPS) → `merge.py` (concatenate ranges) → `backtester/snapshots/`

| File | Purpose |
|------|---------|
| `recorder.py` | Main asyncio daemon, burst snapshot cycle |
| `ws_client.py` | Persistent unauthenticated WebSocket to Deribit |
| `snapshotter.py` | 5-min aggregation, daily parquet writer, crash recovery |
| `instruments.py` | REST instrument discovery, change callbacks |
| `health.py` | HTTP `/health` endpoint, Telegram alerts (throttled) |
| `config.py` | `RECORDER_*` env vars |
| `sync.py` | rsync download from VPS |
| `merge.py` | Combine daily parquets into range files |

**Deployment:** systemd `ct-recorder.service`. Env: `RECORDER_HEALTH_PORT` (8090), `RECORDER_DATA_DIR`, `RECORDER_SNAPSHOT_INTERVAL_MIN` (5).

---

## 10  Indicators Module (`indicators/`)

Python ports of TradingView Pine Script indicators. Source Pine files live in `/Users/ulrikdeichsel/WorkspacePineStrategy/indicators/`.

| File | Description |
|------|-------------|
| `turbulence.py` | Composite 0–100 turbulence score (Parkinson RV, trend, burst, decay). Entry condition factory. Ported from `market_wildness_v2.pine`. |
| `supertrend.py` | SuperTrend indicator port |
| `data.py` | Shared data layer: `fetch_klines()` from Binance |

Pattern: pure transform function + `get_X_now()` wrapper. Never fetch data inside the transform.

---

## 11  Environment & Config

```bash
# .env (gitignored)
DEPLOYMENT_TARGET=development          # 'development' (macOS) or 'production' (Ubuntu VPS)
TRADING_ENVIRONMENT=testnet            # 'testnet' or 'production'
EXCHANGE=coincall                      # 'coincall' or 'deribit'

# Coincall
COINCALL_API_KEY_TEST=...
COINCALL_API_SECRET_TEST=...
COINCALL_API_KEY_PROD=...
COINCALL_API_SECRET_PROD=...

# Deribit
DERIBIT_CLIENT_ID=...
DERIBIT_CLIENT_SECRET=...
DERIBIT_BASE_URL=https://test.deribit.com

# Services
TELEGRAM_BOT_TOKEN=...
TELEGRAM_CHAT_ID=...
DASHBOARD_PASSWORD=...
DASHBOARD_PORT=8080
DASHBOARD_MODE=full                    # full | control | disabled

# Slot system
SLOT_STRATEGY=daily_put_sell
SLOT_NAME=slot-01
PARAM_QTY=0.8                          # strategy param override
```

**API URLs:**
- Coincall: testnet `https://betaapi.coincall.com`, prod `https://api.coincall.com`
- Deribit: testnet `https://test.deribit.com`, prod `https://www.deribit.com`

---

## 12  Test Suite

**Fast tests:** `python -m pytest tests/ -v` (~1.7s, 244 tests)
**Live tests:** `python -m pytest tests/live/ -m live -v` (Deribit testnet, needs env vars)

Config in `pyproject.toml`: `addopts = "-m 'not live'"` auto-deselects live tests.
Shared fixtures in `tests/conftest.py`: `MockExecutor`, `MockMarketData`, `make_account()`, `make_position()`.

Fast tests: `test_order_manager`, `test_execution_params`, `test_trade_lifecycle`, `test_conditions`,
`test_option_selection`, `test_persistence`, `test_reconciliation`, `test_dashboard`,
`test_fill_manager`, `test_lifecycle_engine`, `test_strategy_runner`, `test_deribit_symbols`

Live tests: `tests/live/test_deribit_integration.py` — 25+ tests against Deribit testnet (marked `pytest.mark.live`).

---

## 13  Resilience

- **Request timeouts:** 30s on every API call
- **@retry:** Exponential backoff (1→2→4s) for ConnectionError/Timeout only
- **Error isolation:** Main loop tolerates 10 consecutive errors, then exits + Telegram alert
- **Trade persistence:** Active trades → `logs/trades_snapshot.json` every tick; completed → `logs/trade_history.jsonl`
- **Trade recovery:** On startup, verifies snapshot against exchange positions, re-attaches exit conditions
- **Kill switch:** `PositionCloser` two-phase close (mark → aggressive ±10%)
- **Process supervision:** systemd on production (auto-restart)

---

## 14  Coding Conventions

- `Optional[X]`, not `X | None` (Python 3.9 compat)
- Dataclasses everywhere, frozen for thread-safe snapshots
- Factory functions for conditions — return callables, not classes
- Logging via `logging.getLogger(__name__)` in every module
- Fire-and-forget for Telegram — never crashes the bot
- Side as string (`"buy"` / `"sell"`) in all internal code
- `_p("NAME", default)` for strategy params from env vars

---

## 15  Key Documents

| Document | Content |
|----------|---------|
| `AGENTS.md` | Full holistic orientation (authoritative) |
| `docs/MODULE_REFERENCE.md` | Classes, methods, dataclasses |
| `docs/API_REFERENCE.md` | Exchange REST API endpoints |
| `deployment/UBUNTU_DEPLOYMENT.md` | VPS setup, systemd, deployment guide |
| `backtester/README.md` | Backtester workflow and research flow |
