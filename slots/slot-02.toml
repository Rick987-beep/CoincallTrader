# ===========================================================================
# Slot 02 — Short Straddle / Strangle (Deribit)
#
# Backtester2 combo #1 (score 0.983): offset=$1500, entry=12:00 UTC,
# SL=250%, max_hold=16h — $2,480 PnL, 14 trades, 79% win rate, Sharpe 18.23.
#
# Edit params below, then deploy:
#   ./deployment/deploy-slot.sh 02
# ===========================================================================

name = "Short Straddle Strangle"
strategy = "short_straddle_strangle"
account = "deribit-main"

[params]
qty = 0.8
offset = 1500
entry_hour = 12
stop_loss_pct = 2.5
max_hold_hours = 16
check_interval = 15
